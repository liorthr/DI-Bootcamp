import Button from "./Button";

const Action = (props) => {
    return(
        <>
        <Button countAction={props.countAppButton} setCountAction={props.setCountApp}/>
        
        </>
    )
}
export default Action;